# 🌐 局域网访问配置说明

## ✅ 配置完成

系统已成功配置为支持局域网访问！

---

## 📱 访问地址

### 本机访问
```
http://localhost:5173
```

### 局域网访问（其他设备）
```
http://192.168.8.168:5173
http://192.168.3.85:5173
http://172.16.31.1:5173
http://172.16.244.1:5173
```

**选择其中一个可用的IP地址即可**

---

## 🚀 启动方式

### 方式1: 使用局域网启动脚本（推荐）
```bash
./start-network.sh
```

### 方式2: 使用普通启动脚本
```bash
./start.sh
```

两种方式都支持局域网访问！

---

## 💡 使用场景

### 场景1: 办公室内多人使用
- 服务器电脑运行系统
- 其他同事通过局域网IP访问
- 共享同一个数据库

### 场景2: 车间/仓库扫码
- 固定电脑运行系统
- 扫码员使用平板/手机访问
- 通过WiFi连接

### 场景3: 移动办公
- 笔记本作为服务器
- 手机/平板作为扫码终端
- 创建热点或连接同一WiFi

---

## 🔧 配置说明

### 后端配置 (backend/src/app.ts)
```typescript
// 监听所有网络接口
const HOST = '0.0.0.0';

// CORS 允许所有来源
app.use(cors({
  origin: '*',
  credentials: true
}));
```

### 前端配置 (frontend/vite.config.ts)
```typescript
server: {
  host: '0.0.0.0', // 允许局域网访问
  port: 5173
}
```

---

## 🔒 安全建议

### 开发/内网环境（当前配置）
- ✅ CORS 允许所有来源
- ✅ 适合局域网使用
- ✅ 快速部署

### 生产环境建议
```typescript
// 限制特定域名
app.use(cors({
  origin: ['http://192.168.1.100:5173', 'http://yourdomain.com'],
  credentials: true
}));
```

---

## 🛠️ 防火墙设置

### macOS
```bash
# 允许端口
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/local/bin/node
```

### Windows
```
控制面板 → Windows Defender 防火墙 → 高级设置 → 入站规则
添加端口 5173 和 3001
```

### Linux
```bash
# 允许端口
sudo ufw allow 5173
sudo ufw allow 3001
```

---

## 📊 网络接口说明

您的电脑有多个网络接口：

| IP地址 | 说明 | 推荐 |
|--------|------|------|
| 192.168.8.168 | WiFi/以太网 | ⭐⭐⭐ |
| 192.168.3.85 | WiFi/以太网 | ⭐⭐⭐ |
| 172.16.31.1 | 虚拟网卡 | ⭐ |
| 172.16.244.1 | 虚拟网卡 | ⭐ |

**建议使用 192.168.x.x 开头的地址**

---

## 🔍 故障排查

### 问题1: 无法访问
**检查清单**:
- [ ] 设备是否在同一局域网
- [ ] 防火墙是否允许端口
- [ ] IP地址是否正确
- [ ] 服务是否正常运行

**解决方案**:
```bash
# 检查服务状态
./status.sh

# 查看日志
tail -f backend.log
tail -f frontend.log
```

### 问题2: 连接超时
**可能原因**:
- 防火墙阻止
- 网络隔离
- IP地址错误

**解决方案**:
```bash
# 测试端口连通性
telnet 192.168.8.168 5173
```

### 问题3: API请求失败
**检查**:
- 后端服务是否运行
- CORS配置是否正确
- 网络代理设置

---

## 📱 移动设备访问

### iOS/Android
1. 连接同一WiFi
2. 打开浏览器
3. 输入: `http://192.168.8.168:5173`
4. 添加到主屏幕（可选）

### 平板电脑
- 横屏模式体验更佳
- 支持触摸操作
- 可使用蓝牙扫码枪

---

## 🎯 性能优化

### 局域网环境
- 延迟: < 10ms
- 速度: 快速响应
- 稳定性: 高

### 建议
- 使用有线网络（更稳定）
- 5GHz WiFi（更快）
- 避免网络拥堵时段

---

## 📞 技术支持

### 查看IP地址
```bash
# macOS/Linux
ifconfig | grep "inet "

# Windows
ipconfig
```

### 查看端口占用
```bash
# macOS/Linux
lsof -i :5173
lsof -i :3001

# Windows
netstat -ano | findstr :5173
```

### 重启服务
```bash
./stop.sh
./start-network.sh
```

---

## ✨ 总结

✅ **已配置完成**:
- 后端监听 0.0.0.0:3001
- 前端监听 0.0.0.0:5173
- CORS 允许所有来源
- 自动显示局域网IP

✅ **可以使用**:
- 本机访问
- 局域网内其他设备访问
- 移动设备访问

✅ **适用场景**:
- 办公室多人协作
- 车间/仓库扫码
- 移动办公

---

**立即体验局域网访问！** 🎊

*配置时间: 2026-05-08*
