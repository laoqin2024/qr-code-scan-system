# 503 错误快速修复指南

## 🚨 问题

访问 http://192.168.8.91:6006 出现 503 错误

## ✅ 快速解决

在服务器上执行以下命令：

```bash
# 方法 1：自动修复（推荐）
cd /opt/scan-code
bash scripts/fix-503.sh
```

或者手动修复：

```bash
# 方法 2：手动修复
# 1. 检查后端服务
pm2 list

# 2. 如果后端未运行，启动它
cd /opt/scan-code/backend
pm2 start npm --name "scan-code-backend" -- start
pm2 save

# 3. 查看日志确认启动成功
pm2 logs scan-code-backend --lines 20

# 4. 重启 Nginx
sudo systemctl restart nginx

# 5. 测试访问
curl http://localhost:6006
```

## 🔍 原因分析

503 错误通常是因为：
- ✅ Nginx 正常运行并监听 6006 端口
- ❌ 后端服务（3001 端口）未启动或崩溃

Nginx 配置是正确的，问题在于后端服务。

## 📋 验证修复

```bash
# 1. 确认后端运行
pm2 list
# scan-code-backend 应该显示 online

# 2. 确认端口监听
netstat -tlnp | grep 3001
# 应该看到 node 进程

# 3. 测试后端 API
curl http://localhost:3001/api/health
# 应该返回 {"status":"ok"}

# 4. 浏览器访问
# http://192.168.8.91:6006
```

## 📚 详细文档

如需更多帮助，查看：
- [Nginx 503 错误排查指南](docs/NGINX_503_TROUBLESHOOTING.md)
- [诊断脚本](scripts/diagnose-503.sh)
